const images = {
  bitcoin: require('./bitcoin.svg').default,
  bag: require('./bag.svg').default,
  stocks: require('./increasing_stocks.svg').default,
  shield: require('./shield.svg').default,
  charts: require('./charts.svg').default,
  arrows: require('./arrows.svg').default,
};

export default images;
